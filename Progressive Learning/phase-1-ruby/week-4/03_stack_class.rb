# PROBLEM: Stack (OOP)
# Implement a Stack class using an Array internally.
#
#   - push(item)   — add to the top
#   - pop          — remove and return the top item; return nil if empty
#   - peek         — return the top item without removing it; return nil if empty
#   - empty?       — return true if the stack has no items
#   - size         — return number of items
#
# P —
#
# E —
#   s = Stack.new
#   s.empty?     #=> true
#   s.push(1)
#   s.push(2)
#   s.peek       #=> 2
#   s.pop        #=> 2
#   s.size       #=> 1
#   s.pop        #=> 1
#   s.pop        #=> nil
#
# D —
#
# A —
#   1.
#   2.
#   3.
#
# C —
class Stack
  def initialize
  end

  def push(item)
  end

  def pop
  end

  def peek
  end

  def empty?
  end

  def size
  end
end
